import { Typography, Box } from "@mui/material";

const APICard = () => {
  return (
    <Box>
      <Typography variant="body1" gutterBottom>
        API and Services is a new service by Amirhossein Mohammadi. You can see
        provided services and status of them in services site.
      </Typography>
      <Typography variant="body1" gutterBottom>
        You can also use these APIs of you want.
      </Typography>
      <br />
      <Typography variant="body1" gutterBottom>
        For more information, head over to amirhossein.app.
      </Typography>
    </Box>
  );
};

export default APICard;
